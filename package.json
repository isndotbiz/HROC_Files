{
  "name": "hroc-site-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
